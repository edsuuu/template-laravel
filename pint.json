{
    "preset": "psr12",
    "exclude": [
        "database/migrations"
    ],
    "rules": {
        "ordered_imports": {
            "sort_algorithm": "alpha",
            "imports_order": ["class", "function", "const"]
        }
    }
}
